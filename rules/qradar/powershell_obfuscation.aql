SELECT * FROM events 
WHERE Image ILIKE '%powershell.exe' 
AND (CommandLine ILIKE '% -enc %' OR CommandLine ILIKE '% -EncodedCommand %' OR CommandLine ILIKE '% -e %' OR CommandLine ILIKE '% -en %')
